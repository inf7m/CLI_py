Building a CLI application with python - using typer

At the start point - i've decided to use this CLI tool on RHEL8/9

Contains at the first stage with

checking command:
  dependency
  network
 
 setup command:
  dockerContainer

